<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />

  <script src="https://cdn.tailwindcss.com"></script>
  <title>Query List</title>
</head>
<body class="bg-gray-900 text-white">
  <div class="container mx-auto p-4">
    <div class="mt-8 px-4 flex flex-col">
      <h2 class="text-4xl font-bold mb-8 text-white">Queries List</h2>
      <% if (data && data.length > 0) { %>
      <ul class="space-y-4">
        <% data.forEach((query) => { %>
        <li class="bg-gray-800 border border-gray-700 rounded-lg p-6 shadow-lg">
          <h3 class="text-2xl font-semibold mb-4 text-white">
            Query By: <%= query.name %>
          </h3>
          <div class="space-y-2">
            <p>
              <span class="font-bold text-gray-300">Hi! My name is</span> <%= query.name %>,
            </p>
            <p>
              <span class="font-bold text-gray-300">and I represent</span> <%= query.company %>.
            </p>
            <p>
              <span class="font-bold text-gray-300">I am looking for a partner to help me with</span> <%= query.goal %>.
            </p>
            <p>
              <span class="font-bold text-gray-300">I aim to have this completed by</span> <%= query.date %>.
            </p>
            <p>
              <span class="font-bold text-gray-300">and would like to keep the budget around</span> <%= query.budget %> USD.
            </p>
            <p>
              <span class="font-bold text-gray-300">You can reach me at</span>
              <a class="text-blue-400" href="mailto:<%= query.email %>"><%= query.email %></a>
            </p>
            <p>
              <span class="font-bold text-gray-300">To start the conversation. Optionally, I'm sharing more details:</span>
              <br>
              <%= query.productDetails %>
            </p>
          </div>
        </li>
        <% }); %>
      </ul>
      <% } else { %>
      <p class="text-white">No queries found.</p>
      <% } %>
    </div>
  </div>
</body>
</html>
